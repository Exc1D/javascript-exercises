const sumAll = function (min, max) {
  // Error handling if number is not entered
  if (!Number.isInteger(min) || !Number.isInteger(max)) return "ERROR";
};

// Do not edit below this line
module.exports = sumAll;
